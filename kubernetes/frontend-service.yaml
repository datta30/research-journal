apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: research-journal
spec:
  selector:
    app: frontend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer  # Expose to external traffic
